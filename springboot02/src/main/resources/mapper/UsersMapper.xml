<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.learnspringboot.mapper.UsersMapper">
    <!--新增-->
    <insert id="insertUser" parameterType="users">
        insert into users(name,age,password,email)values(#{name},#{age},#{password},#{email});
    </insert>
    <!--查询列表-->
    <select id="selectUsersAll" resultType="users">
        select id,name,age,email from users;
    </select>
    <!--查询单个用户-->
    <select id="selectUsersById" resultType="users">
        select id,name,age,email from users where id =#{value};
    </select>
    <!--更新用户-->
    <update id="updateUser" parameterType="users">
    update users set name=#{name},age=#{age} where id=#{id};
    </update>
    <!--删除用户-->
    <delete id="deleteUserById" >
        delete from users where id =#{value};
    </delete>
</mapper>
